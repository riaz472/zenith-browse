
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.riaz.zenithbrowse',
  appName: 'Zenith Browse',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
